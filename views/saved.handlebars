<div class="container-fluid">
    <nav class="navbar">
    </nav>
    <div class="container">
        <div class="jumbotron">
            <h1>Saved Notes</h1>
            <hr>
            <a href="/"> Home </a>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <table id="results">
                    <thead>
                        <tr>
                            <th id="NoteAuthor">Author</th>
                            <th id="note">Note</th>
                            <th id="headline">Headline</th>
                            <th id="summary">Summary</th>
                            <th id="byline">Byline</th>
                            <th id="articleImage">Image</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each article}}
                        <tr><td>{{note.author}}</td>
                        <td>{{note.body}}</td>
                        <td><a src="{{article.url}}">{{article.headline}}</a></td>
                        <td>{{article.summary}}</td>
                        <td>{{article.byline}}</td>
                        <td><img src="{{article.photo}}"></td></tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>